export default function Header({name}) {
    return (
        <div className="bg-blue-100 w-full max-w-7xl mx-auto py-4">
            <h1 className="font-bold text-4xl">{name} page</h1>
        </div>
    )
}